length -> strlen(string-name);
int input[100];
cin >> input;
int len = strlen(input);
cout << len << endl;

2) comparing two strings.
strcmp(input1, input2);

0 -> equal
nonzero -> unequal

3) copy string
strcpy(destination-stirng, source string)
strcpy(input1, input2);

4)strncpy(input1, input2, 3);

NULL ko copy nhi karega.
